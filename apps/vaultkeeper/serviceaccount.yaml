apiVersion: v1
kind: ServiceAccount
metadata:
  name: vaultkeeper
  namespace: vaultkeeper
  labels:
    app: vaultkeeper-server
# VaultKeeper server does not interact with the Kubernetes API.
# Disable token auto-mount to shrink the blast radius of a pod compromise.
automountServiceAccountToken: false
