apiVersion: v1
kind: ServiceAccount
metadata:
  name: phantom-site
  namespace: phantom-site
  labels:
    app: phantom-site
# Phantom-site nginx pod does not call the Kubernetes API.
# Disable token auto-mount so a leaked pod cannot reach the API server.
automountServiceAccountToken: false
