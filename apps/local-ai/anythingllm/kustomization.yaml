apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: anythingllm

# Directory-relative references (assumes this file is in apps/local-ai/anythingllm/)
namespace: local-ai

resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml

commonLabels:
  app: anythingllm
  managed-by: argocd

# Image pinning deferred: anythingllm image not yet published (deployment disabled with replicas: 0).
# When publishing a build, add an `images:` override below to pin the tag.
