apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Directory-relative references (assumes this file is in apps/local-ai/homarr/)
namespace: local-ai

resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml

commonLabels:
  app: homarr
  managed-by: argocd

# TODO: Configure image tag replacement if using separate build/deploy workflows
# images:
#   - name: ghcr.io/realphantomlee/homarr
#     newTag: v1.2.3
